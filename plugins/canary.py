import time
outputs = []

def canary():
    #NOTE: you must add a real channel ID for this to work

    # outputs.append(["C0EA4CNRG", "bot started: " + str(time.time())])
    outputs.append(["D0EA1QUE7", "bot started: " + str(time.time())])

canary()
